# State

- **Current phase**: 03 Voice Training (with a delivered Phase 02/03 integration slice)
- **Status**: Plan 03-01 complete; Plan 03-02 ready
- **Last action**: Fixed the intermittent completed-STT-to-Script handoff. Terminal progress snapshots no longer stop polling before the authoritative media asset (transcript, words, revisions, and updated timestamp) has loaded. Added a delayed-response regression test; 42 frontend and 57 backend tests, production build, and live restart/data checks passed.
- **Next action**: Round R2 in `docs/STABILIZATION-LOG.md` (per-word timing trust and phrase-edge snapping). W0 and all of W1 are done on branch `fix/w1-runtime-and-index-performance`: the launcher now owns every workload process through a kill-on-close Job Object, word timings live outside the shared index (media.list 3.3s to 0.094s, index.json 31.3MB to 3.05MB), diarization progress no longer rewrites the project, and the workspace stopped re-rendering itself while polling. Then execute `.planning/phases/03-voice-training/03-02-PLAN.md`.
- **Blockers**: No functional blocker. Browser live transcript depends on browser SpeechRecognition; detailed STT remains local/background through the configured Studio adapter. AI review requires endpoint, model, and API key in Windows → Preferences.
- **Decisions**: The folder containing project.json is the portable project aggregate; app registry and runtime preferences are machine-local. Media Pool and Sound Library are tabs in one left panel. Sound Library owns Speaker and Environment profiles, while both can be attached at footage and word level. OmniVoice remains untouched; its language/facet schema is exposed through the application adapter. Finalized STT trusts only processor-provenanced word timing; Vietnamese uses non-batched Faster-Whisper DTW plus an unpadded acoustic-boundary pass because VAD padding shifted whole phrases early. A terminal STT progress snapshot is not the Script payload: the UI must keep the job active until the full persisted asset is fetched. A tiny control plane must remain alive when workloads are off, otherwise an in-app Turn on command is impossible.
