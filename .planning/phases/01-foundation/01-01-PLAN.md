# Phase 01 Execution Plan

1. Create FastAPI project and engine modules behind `ProjectRepository` and `VoiceEngine` interfaces.
2. Add repository tests first for project persistence and engine status.
3. Create React app shell, domain types, API client, module registry, and shared design tokens.
4. Build Project Hub and persist create/open behavior through the backend.
5. Compose the three workspace pages from shared modules and add column resizing.
6. Add focused frontend tests for page capability contracts and Script editing.
7. Add launch/update scripts, install isolated dependencies, build, and browser-verify.

