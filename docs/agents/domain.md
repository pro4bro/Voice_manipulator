# Domain Documentation

This repository uses a single domain context. Read root `CONTEXT.md` before naming domain types or interfaces. Record hard-to-reverse, surprising trade-offs under `docs/adr/`.

