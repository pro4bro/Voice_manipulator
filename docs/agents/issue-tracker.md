# Issue Tracker

Issues are local markdown files under `.scratch/<feature>/` until this repository has a remote issue tracker. Specs and tickets must use stable requirement IDs from `.planning/REQUIREMENTS.md`.

